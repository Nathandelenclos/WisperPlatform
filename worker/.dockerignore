# Build context of the worker image (`worker`).

__pycache__
*.pyc

# Tests run in CI; the image bundles neither the test suite nor the whisper stub.
tests

# Environment files: never in a layer.
.env
.env.*

Dockerfile
.dockerignore
