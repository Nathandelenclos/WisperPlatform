/** Panneau principal tant qu'aucune transcription n'est ouverte. */
export function NoSelection() {
  return (
    <p className="empty empty--main">
      Choisissez une transcription dans la liste pour lire le média, corriger le texte et
      l'exporter.
    </p>
  );
}
